# env / secrets
.env
*.env
!.env.example

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# node (if UI)
node_modules/
dist/

# os
.DS_Store

# obsidian volatile state
**/.obsidian/workspace*.json

# internal panel / study prep — kept out of the submission
Vantage-vault/08-Panel-Prep.md
Vantage-vault/14-Study-Guide.md

# local caches
.cache/

# runtime-authored skills (Flow 1) — live on a volume / local dir, not committed
app/skills_authored/
