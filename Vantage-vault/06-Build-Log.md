---
title: Build Log
type: build-log
status: active
updated: 2026-05-30
---

# Build Log

> [!NOTE] Purpose
> Thin-slice plan + dated progress + the AI-usage log.

## Thin-slice plan
- [x] **Slice 0** — Codespace + Keycloak + Redis. ✅
- [x] **Slice 1a** — auth gate (JWT verify; forged/missing → 401). ✅
- [x] **Integration** — full stack on `main`; seed loads (12/40/132/14). ✅
- [x] **Slice 1b** — first tool (`get_customer_profile`) **+ agent loop** (`POST /ask`, Claude tool-calling); validated found/ambiguous/not-found. ✅
- [x] **Slice 2** — read tools `get_open_issues`, `summarise_issue_history` (PR #4); **custom MCP server** (Streamable HTTP) + agent flipped to an **MCP client** (PR #5, #6). MCP Must met; API holds no DB access. ✅
- [x] **Slice 3** — write tools + RBAC **denial** case (PR #7); **Redis** multi-turn session memory (PR #8). ✅
- [~] **Slice 4** — reusable **Skill runner** + seeded **Escalation Summary** (PR #9); user **Skill authoring** Flow 1 endpoints — draft-from-session + save (PR #10); the `/ask` authoring trigger (Flow 1 PR B) — *next*. Differentiator beyond the Must.
- [x] **Slice 5** — eval set (10/10, PR #11) + observability (trace + latency + audit logs) + full README/diagram/AI-usage (PR #12) + minimal **UI** + real backend + streaming (PR #17, #18, #20, #22, #23). ✅
- [x] **Slice 6** — browse tools `list_customers` / `list_issues` / `list_next_actions` (PR #21) + 75-test Playwright UI suite (PR #23). ✅
- [x] **Slice 7** — harness-quality pass: doc-accuracy fixes (PR #27) + **0 open CodeQL alerts** (PR #28); a UX series from a "copilot = harness" analysis — real Stop + mock-engine deletion + copy (PR #29), `/followups` + `/sessions/{id}/title` LLM endpoints (PR #30), grounding **Sources footer** (PR #31), **role-aware follow-up chips + async chat titles** (PR #32). ✅
- [~] **Slice 8** — first two **Could** items: the **proactive admin briefing** (LangGraph hybrid + HITL approval, ADR-008) + **bonus tracing** (Phoenix/OTel + LangSmith). Core + tracing built and validated on `feat/proactive-briefing-langgraph` (briefing eval 11/11, acceptance 13/13 unchanged); admin UI button + PR merge pending.

## Progress log (newest first)
### 2026-06-01
- ✅ **Proactive admin briefing — LangGraph hybrid + HITL + tracing (branch `feat/proactive-briefing-langgraph`, ADR-008).** Picked up the first two **Could** items after the Musts/Shoulds. The reactive loop strains on exactly three things — parallel fan-out, a human approval gate, durable pause/resume — and the briefing needs all three, so it's a **LangGraph `StateGraph`** *alongside* the loop (not a rewrite): `plan` (rank the high-risk fleet via the new admin-only `get_high_risk_customers`) → `summarise_one` (map: the escalation-summary skill fanned out per account via `Send`) → `detect` (cross-customer clusters via the new `detect_patterns`) → `draft` (one next action per account, each tied to a real fetched issue id) → `interrupt()` (admin approval) → `route` (write approved drafts with the **approver's** token — the approval is the authorisation). Two new **admin-only** MCP tools (RBAC at the boundary like the writes, audited); the graph is just another MCP client, so tools/RBAC/MCP/skills carry over unchanged. **Checkpointer = Redis** (image → `redis-stack-server`, a superset; the api stays free of the Postgres system-of-record — ADR-003/006). **Bonus tracing:** OpenTelemetry → self-hosted **Phoenix** auto-instruments the OpenAI SDK (loop + briefing) and LangChain/LangGraph (the graph renders as a span tree — `LangGraph → plan/fan_out/summarise_one×4/detect/draft/approval`); **LangSmith** wired opt-in (key-gated). Whole feature behind a **guarded import** so it can never affect `/ask` or the UI. **Validated on the Codespace:** briefing eval **11/11**, acceptance **13/13** (reactive intact on the `openai` 2.x bump from the rebuild), smoke test PASSED (11 tools + new-tool RBAC matrix), durable Redis checkpoint keys + Phoenix span tree both confirmed. **UI (S6):** an admin-only modal that **streams the graph's progress** (`GET /briefing/stream` via LangGraph `astream` → a node-by-node checklist that lights up as each account is summarised, under an animated Vantage brand mark — fixes the tens-of-seconds blank wait) → review drafts → approve. Playwright **86/86** (the existing 75 + 5 new briefing tests + the streaming flow; also fixed one pre-existing flaky chip/message-collision test). **PR #38 open + green** (CI + CodeQL ×3).
### 2026-05-30
- ✅ **Follow-up chips + async LLM chat titles (PR #32, frontend).** Wires the #30 backend into the UI. After each clean answer, `runAgent` fires `_loadFollowups` → `POST /followups` → up to 3 role-aware suggestion chips under the latest answer (`idx === messages.length - 1`); a click sends it. For a new chat, `_generateTitle` → `POST /sessions/{id}/title` (async, non-blocking) replaces the placeholder first-message title with the LLM one (persisted server-side, so sidebar + reload reflect it). Both fire-and-forget; degrade silently.
- ✅ **Grounding citations — Sources footer (PR #31).** A visible "Sources" list under each answer citing the tools + data it draws on (`tool(args) → status · summary`), so "where is this grounded?" is answered without opening the thinking panel. The `done` SSE event already carried the full backend trace *with results* but the UI was discarding it — the handler now enriches each trace entry via `_sourceSummary` ("4 open", "12 total", …).
- ✅ **Assistive API: `/followups` + `/sessions/{id}/title` (PR #30, backend).** Two auth-gated endpoints reusing the `app/llm.py` adapter via a new no-tools `agent.complete` helper. Follow-ups are **role-gated** (never suggest a write the caller's role can't do); titles persist to the existing `session:{id}:title` Redis key (so GET /sessions needs no shape change). Both degrade gracefully (empty list / fallback title). Backend designed in parallel via a **worktree subagent** — which proved read-only here, so it returned exact diffs that were applied + verified in the main session.
- ✅ **Delete dead mock engine + real Stop + copy-answer (PR #29).** `app/web/app.js` still carried the prototype's scripted mock agent (`routeQuery`/`seedChats`/`heroConversation`/…/`ROLE_TOOLS`), unreferenced since the UI went backend-driven — removed **277 lines** + fixed the stale `// prototype-app.js — No backend` header. **Stop** was cosmetic (the SSE reader kept appending after `cancel()`); now an `AbortController` is wired into the `/ask/stream` fetch and aborted in `cancel()`, with `AbortError` treated as an intentional stop. Added a copy-answer button. (Deferred fast-follow: copy code-block.)
- ✅ **0 open CodeQL alerts (PR #28).** Resolved the 4 open code-scanning alerts: least-privilege `permissions:` on both workflows; stack-trace exposure in `/ask/stream` (+ the two auth-callback handlers) → log server-side, return a generic detail; open-redirect in `/auth/switch` → persona allow-list guard (the residual alert is a verified false-positive, dismissed with a written justification). Added a `vantage.api` logger.
- ✅ **Vault doc-accuracy fixes (PR #27).** Corrected drift in the docs vs the implementation: tool count 8→**9**, eval cases 10→**13**, seed counts made exact (12/40/**132**/**14**) across 00-NOW / 04-Architecture / 07-Evals / ADR-005 / 01-Frame / 13-Integration + two code docstrings. Left this log's historical "10/10, PR #11" entries intact (correct as of then).
### 2026-05-29
- ✅ **Persona logins + real role switch + reasoning-in-trace + UI bug fixes (PR #23).** Five things that tangled together while polishing the demo UX. (a) Keycloak users renamed to the personas the vault stories already use — `priya.nair` / `priya` (sales), `marcus.webb` / `marcus` (support), `dana.okafor` / `dana` (admin); UI derives display name + initials from the dot-separated username (`marcus.webb` → "Marcus Webb", "MW"). (b) The role-switcher menu used to flip a frontend `this.role` variable — the JWT, Redis session, and realm roles never changed (server-side RBAC was correct, so it was safe; UX was misleading). Rewired: clicking a role hits `GET /auth/switch?username=<persona>` → clears the BFF session + revokes the Keycloak refresh token + drops the cookie → 302 to `/auth/login?prompt=login&login_hint=<persona>` → Keycloak forces re-auth → fresh JWT with the new realm roles. **One click = one real identity swap.** (c) Each assistant message now has a `timeline` interleaving the model's pre-tool narration ("thought" blocks) with tool-call pills; the frontend buffers text deltas and on `tool_start` commits the buffer as a thought + retracts the matching tail from `ass.md`, so only the *final-iteration* text remains as the answer. System prompt nudges the model to write 1–2 sentence intent narration before each tool call. (d) "Answer disappears, comes back when you switch chats" bug fixed — `loadSessions()` was wholesale-replacing `this.chats` with fresh objects (`messages: []`, `loaded: false`) when a new chat completed, wiping the just-streamed assistant turn from memory; now MERGES by id. (e) No emojis from the LLM — system prompt forbids them, defensive `EMOJI_RE` strip in `renderMd()` as belt-and-braces. Verified via the 75-test Playwright UI suite (`tests/ui/test_ui.py`, 14 categories) — **all 75 green** end-to-end against the Codespace stack.
- ✅ **Real SSE streaming via `/ask/stream` (PR #22).** Token-level streaming for the UI. Event types: `session` (UI adopts the server's session id before any content arrives), `text` (delta), `tool_start` (`{tool, input}`), `tool_end` (`{tool, status, ms}`), `done` (`{trace, elapsed_ms}`), `error`. Same agent loop as `/ask`; the OpenAI SDK's `stream=True` is wrapped to accumulate tool_call deltas across chunks. The non-streaming `/ask` stays for the eval harness (deterministic for assertions); `/ask/stream` is the UI surface. Persistence runs in `finally:` so partial answers from interrupted streams are still saved.
- ✅ **Browse tools (PR #21).** `list_customers` / `list_issues` / `list_next_actions` — filterable + paginated reads behind the MCP server. Mirrors the realistic v1 expansion: a user can ask "who are our customers?", "show me critical issues this week", "what's overdue?" without naming a specific id. The agent picks them when the user wants to browse, picks the targeted tools otherwise. Filters: customer name, region, tier, category, priority, status, date ranges; cursor pagination. RBAC at the tool boundary as ever.
- ✅ **UI wired to real backend (PR #20).** Frontend now talks to real `/ask/stream` + `/sessions` + `/skills/*` — no more mocked `routeQuery()` router. Sidebar chat history, save-as-skill modal, skill runner, multi-turn — all real. Skills authored from a session show up in the menu instantly.
- ✅ **OIDC Auth-Code + PKCE + BFF cookie (PR #18).** The chat UI's auth got upgraded from "dev ROPC tokens in the browser" to real production auth: `/auth/login` redirects to Keycloak with PKCE; `/auth/callback` exchanges the code server-side; tokens never reach the browser. The httpOnly `vantage_sid` cookie names a Redis-backed `auth_session:{sid}` (stored as access+refresh tokens + verified identity). `/auth/whoami` is the UI's bootstrap call. `/auth/logout` clears both ends and best-effort revokes the Keycloak refresh token. Cookie attributes (`secure`, `samesite`) configurable for the Codespaces HTTPS forward.
### 2026-05-28
- ✅ **Chat UI foundation (`app/web/`)** — lifted the Claude Design handoff (`prototype.html` + `prototype-app.js`) into `app/web/` as `index.html` + `app.js`; FastAPI mounts the dir at `/web` and serves `index.html` at `/`. Stack: Alpine.js + Tailwind Play CDN + markdown-it + DOMPurify, no build step. Dark theme default with light toggle; ⌘N / ⌘K shortcuts; dev role-switcher in the avatar menu (sales/support/admin) — will become a Keycloak JWT claim once OIDC lands. **Backend is mocked client-side** (the `routeQuery()` router in `app/web/app.js`); next PR wires real auth-code OIDC + streaming `/ask/stream` + `/sessions` + `/skills/*`. Design source: Anthropic Design URL `XXjxfthWkPg44hDFE44D6Q` — three aesthetic variations on the canvas; ships Direction A · Studio (warm + terracotta, Geist).
- ✅ **LLM client switched to OpenAI SDK + `base_url` (PR #16)** — ADR-001's model-agnostic claim was previously delivered only at the client-instantiation level; the agent loop still spoke Anthropic shapes. Now `app/llm.py` uses `AsyncOpenAI(base_url=..., api_key=...)` and `agent.py`/`skills.py` use OpenAI Chat-Completions shapes (`tool_calls`, `role:"tool"` results). Provider swap is one env-var (`LLM_BASE_URL` + `LLM_MODEL` + `LLM_API_KEY`); default points at Anthropic's OpenAI-compat endpoint so we keep running Claude. LiteLLM considered + rejected (March 2026 PyPI supply-chain compromise on 1.82.7/1.82.8 + documented tool-call/streaming bugs); gateway pattern (Portkey / Vercel AI Gateway / Bifrost) deferred until scale demands it. **Regression: both eval suites pass unchanged — acceptance 10/10, robustness 30/30.**
- ✅ **Robustness coverage (PR #14, #15)** — expanded 02-User-Stories with 20 operational scenarios (S4-S9, SU4-SU9, A3-A6, E4-E7) and added a 30-case **robustness suite** (`eval/run_robustness.py`): grounding edge (typo / single-char / whitespace / non-ASCII / suffix), adversarial prompt-injection (sales pretending to be admin, "ignore prior instructions", fake `<<system>>` tags), out-of-scope, bad inputs (empty / 5000-char / SQL-shaped / null bytes / emoji), multi-turn weirdness, Skill robustness. **30/30 passed**, with two real fixes surfaced and applied: empty `/ask` returned 502 → now 400; tests broadened to accept the model's non-deterministic typo-tolerance (auto-correct *or* clarification). Adversarial probes confirmed: as sales, **no write tool ever succeeded** — RBAC holds at the tool, never the prompt.
- ✅ **A1 next-actions read gap (PR #13)** — user-story walkthrough as a real user surfaced that `create_next_action` worked but no read tool returned next_actions, so A1's "visible on read" failed and the agent couldn't drive `update_next_action` by reference. `summarise_issue_history` now returns `next_actions` too; full create→read→update→re-read loop validated.
- ✅ **Evals + observability + README (PR #11, #12)** — `eval/run_evals.py`: 10 cases (grounding E1/E2/E3, dynamic multi-tool, RBAC allow+deny, multi-turn, the Skill, 401) asserting on the deterministic trace + statuses; **10/10 on the Codespace**. Observability completed: trace now carries per-tool `ms` + total `elapsed_ms` (alongside the existing write audit logs). README rewritten as the front door (architecture diagram, run recipe, endpoints, security, evals). **All core requirements now done;** remaining work is Should/Could (UI, `/ask` save-as-skill trigger, Flow 2).
### 2026-05-27 (cont.)
- ✅ **Skill authoring — Flow 1 endpoints (PR #10)** — turn a finished session into a reusable skill. `/ask` records each turn's tool trace into the session; `POST /skills/draft-from-session` has the LLM generalise the conversation (forced `propose_skill` tool: parameterises inputs like the customer → `{account_name}`), with `allowed_tools` set deterministically from the tools the session used; `POST /skills` saves it to a volume-backed dir (authored override seeded). Any role may author — safe because a skill is a prompt + tool whitelist (RBAC stays in the tools). Proven on the Codespace: session → draft → save → run for a new account.
- ✅ **Reusable Skill runner + Escalation Summary (Slice 4 Must)** — PR #9. Generic engine: a Skill is data (`skills_library/*.json`: name/description/instructions/params/allowed_tools); `run_skill` reuses the agent loop with the skill's instructions as system + tools restricted to its whitelist (least privilege on top of RBAC). Seeded **escalation-summary** (S2 rubric, read-only). Decided the "Skill" question with the user: same concept as Anthropic Agent Skills (packaged reusable capability) but file-based on our raw-API stack, not a `SKILL.md` the Claude runtime loads. Proven on the Codespace as `sales`: Velocity=Critical / Calm Waters=Low / Zzzz=not-found, only whitelisted tools used. **Next:** user Skill authoring (Flow 1 — generalise a finished session into a skill via the history + trace), agreed as the differentiator.
- ✅ **Redis multi-turn memory (Slice 3 PR B → Slice 3 done)** — `/ask` takes a `session_id` (server mints one if absent, returns it); the agent loads/saves the conversation in Redis (`session:{id}`, rolling cap + ~1h TTL), stored as plain user/assistant text turns; lives in the API/agent layer, degrades gracefully if Redis is down (ADR-006). PR #8. Proven on the Codespace: "open issues for Velocity" → "summarise the second one" resolved to issue #3 via context → "what next?" from context; a fresh session carried nothing.
- ✅ **Write tools + RBAC denial (Slice 3 PR A)** — `update_issue` (support+admin), `create`/`update_next_action` (admin) behind the MCP server (PR #7). All 5 tools now exist. Denial enforced **inside the tool** on the re-verified role; returns structured `denied` (no write) + server-side audit log (identifiers only, T7). Writes attributed via `users.keycloak_id = token sub`. Prompt updated so the agent does **not** self-gate (tries the tool, relays the denial) — keeps RBAC out of the prompt (ADR-002). Proven on the Codespace: deterministic role×tool matrix in `smoke_test.py` **and** end-to-end `/ask` (sales→denied-relayed, support→updated).
- ✅ **MCP wired (Slice 2 done)** — custom **MCP server** over Streamable HTTP as its own Compose service (PR #5), then the agent flipped to an **MCP client** that discovers + calls tools (PR #6). Role threaded by **forwarding the Keycloak JWT and re-verifying it at the MCP boundary** (SDK `TokenVerifier`; the bearer middleware 401s a bad token before any tool runs); used the SDK's native resource-server support after a quick introspection spike confirmed the subclassed token survives to the tool. DB access removed from the API (`db.py`/`tools.py` now live in `mcp_server/`; only `security.py` shared). Dir is `mcp_server/` to avoid shadowing the `mcp` SDK package. Validated live: 3 headline `/ask` queries via the MCP path + 401 on no token + `smoke_test.py` rejecting a bad token at the MCP server.
- ✅ **Read tools 2 & 3** — `get_open_issues` + `summarise_issue_history` (PR #4). Shared `_resolve_customer` (get_customer_profile delegates to it — unchanged). Validated end-to-end on the Codespace: the headline query chained all 3 read tools into a grounded escalation summary; E3 zero-open clean; trace returned. (SQL also proven against the committed seed via a throwaway local Postgres while the Codespace was briefly billing-blocked.)
- ✅ **Agentic core working** — `POST /ask` runs a minimal Claude tool-calling loop (ADR-001) over `get_customer_profile`. Validated live: "Velocity"→grounded profile; "Lumen"→asks which (E2, no guess); "Zzzz"→not found (E1, no invention). Merged via PR #3.
- ✅ First tool `get_customer_profile` (PR #2); CI on every PR + branch protection live.
- ✅ Full stack integrated on `main`; auth gate; host pivot Azure→Codespaces.
- ✅ **Data layer landed (PR #1, track/data → main)** — 5-table schema (customers, issues, issue_updates, next_actions, users) + the committed hybrid seed (12/40/132/14): Faker (seeded, pinned 40.19.1) for structure, Claude-authored text for issue descriptions + multi-update histories, deterministic + offline ([ADR-005](05-Decisions/ADR-005-seed-data.md)'s "no runtime LLM dependency"). Eval scenarios planted as data: hero (Velocity = High/Critical), twins (Lumen Commerce / Lumen Commerce Group, distinct account_ref+region), zero-open (Calm Waters), absent name (Zzzz). Data-ci pytest suite — 18 tests + a negative-control — guards every planted scenario, chronology, and that `seed.sql` is still in sync with its generator. Validated against a throwaway local Postgres before integration. Tour: [12-Data](12-Data.md).

### 2026-05-27
- Repo + two parallel tracks; full design locked (ADR-001…006).
### 2026-05-26
- Vault scaffolded.

## AI-usage log
| Date | Task delegated to AI | How I reviewed / validated | Issues caught |
| --- | --- | --- | --- |
| 2026-05-26 | Scaffold vault | against spec | none |
| 2026-05-27 | Architecture research | web-searched best practice; chose tool-loop over LangGraph | AI leaned heavier first |
| 2026-05-27 | Business context | against spec | removed a cross-project "persona" detail |
| 2026-05-27 | Azure provisioning (CLI) | read errors; region/quota diagnostics | my own zsh bug; real cause = SKU lockdown → Codespaces |
| 2026-05-27 | Codespace Docker | inspected creation logs | docker-in-docker feature failed → universal image |
| 2026-05-27 | Auth gate (JWT) | tested valid/missing/tampered tokens | Keycloak issuer mismatch → frontend/backchannel fix |
| 2026-05-27 | Data-track review + integrate | read schema/generator; ran full stack | realm/seed identity mismatch → reconciled |
| 2026-05-27 | Tool + agent loop | ran real /ask queries vs seeded data; checked tool trace | validated grounding (no invented data), E1/E2 behaviour, RBAC, lazy client keeps CI green |
| 2026-05-27 | Read tools 2 & 3 | deep-dived the vault vs the design first; proved SQL on the committed seed (throwaway local PG) then end-to-end on the Codespace | confirmed shared `_resolve_customer` left `get_customer_profile` unchanged; E3 zero-open + headline chain grounded |
| 2026-05-27 | MCP server + agent-as-client | required a design sketch + sign-off before coding; introspection spike on the MCP SDK to ground the auth approach; smoke test + 3 /ask queries on the Codespace | chose forward-JWT + re-verify (not a trusted role header); renamed dir to avoid SDK package shadowing; 401 path confirmed |
| 2026-05-28 | Skill engine + user authoring (Flow 1) | walked through the "what is a Skill" + two authoring UX flows before building; signed off generic runner + file-based storage; ran the full author→save→reuse loop on the Codespace | steered scope vs deadline (build generic, seed Escalation = the Must); kept authoring safe via the tool-whitelist + RBAC |
| 2026-06-01 | Proactive briefing: LangGraph hybrid + HITL + Phoenix/LangSmith tracing (ADR-008) | deep-dived the vault + ADR-001's revisit trigger before building; design sign-off on the hybrid (loop for reactive, graph for proactive) + the prod-faithful Redis-Stack checkpointer; validated end-to-end on the Codespace (briefing 11/11, acceptance 13/13, smoke incl. new-tool RBAC, durable Redis checkpoint + Phoenix span tree) | caught the api-DB-free boundary (ADR-003) → Redis not Postgres checkpointer; rebuilt the *mcp* image too (new tools) when the first briefing came back empty; kept it behind a guarded import so it can't touch the frozen reactive build |
| 2026-05-28 | Evals + README + observability | ran the 10-case harness on the live stack (10/10); reviewed the README against the spec | asserted on deterministic trace not LLM prose; added latency to complete the observability requirement |
| 2026-05-27 | Write tools + RBAC denial | design sketch + sign-off (denial as structured status; slice write vs Redis); deterministic role×tool matrix + end-to-end /ask on the Codespace | insisted the agent must *attempt* the tool (no prompt self-gating) so the denial is genuinely tool-enforced; audit logs identifiers only |
| 2026-05-27 | Data layer (schema + hybrid seed + data-ci) | read the design vault (04-Architecture, ADR-005) first; ran schema+seed in a throwaway PG cluster locally; wrote a pytest suite plus a negative-control that proves the assertions actually fail when a scenario is broken | shaped customers to specific eval scenarios (E1/E2/E3 + hero) instead of random data; pinned Faker so the determinism check stays meaningful; placed tests under db/tests/ to respect the data-track file ownership |
| 2026-05-28 | OIDC Auth-Code + PKCE + BFF cookie (PR #18) | tested full login round-trip on the Codespace HTTPS forward; checked the cookie is httpOnly/secure/samesite=lax; verified the access token never reaches the browser DOM | caught a frontend/backchannel issuer-vs-jwks-url split (Keycloak public hostname vs internal URL); added `prompt=login` later (PR #23) when role-switching needed forced re-auth |
| 2026-05-28 | Wire UI to real backend (PR #20) | replaced the mocked `routeQuery()` with `/ask` + `/sessions` + `/skills/*`; tested multi-turn, save-as-skill, sidebar load | spotted the lazy-load race in `loadSessionMessages` (left an `await` on chat opens); kept the mocked code commented for the design demo only |
| 2026-05-28 | SSE streaming `/ask/stream` (PR #22) | wrote the SSE wrapper around `run_agent_streaming`; tested with the UI's reader-loop frame parser; observed token deltas + tool events arriving live | required adopting OpenAI Chat-Completions tool_call delta accumulation (no helpers in the OpenAI SDK); persistence into `finally:` so interrupted streams still save partials |
| 2026-05-29 | Browse tools (PR #21) | designed the filter+pagination shape; ran cursor-paginated queries against the seed; confirmed the agent picks them appropriately vs the targeted reads | kept the schema parameter-by-parameter (no kwargs splatting) so MCP discovery surfaces clean types to the model |
| 2026-05-29 | Personas + real role switch + reasoning-in-trace + UI bug fixes (PR #23) | drove a 75-test Playwright UI suite to ground each fix in observable behaviour (Alpine state inspection + visible-text assertions); inspected Redis state across user-session keys to confirm RBAC isolation; FLUSHDB'd accumulated test sessions; verified persona round-trip per user; reran full suite to 75/75 | clarified that the role-switcher was *safe but misleading* before rewiring it (server RBAC was always at the JWT — the prompt-side flip never gave real permissions); pinned the model variance in trace-rendering tests by skip-on-no-tools rather than over-engineering retries; nudged the model into a chain-of-thought style via the system prompt without leaking it into the answer area (frontend retracts pre-tool narration into the timeline) |
| 2026-05-29 | Vault doc-accuracy + CodeQL hardening (PR #27, #28) | grepped the repo for stale counts; verified each against source (tools.py decorators, run_evals CASES, seed.sql count==max_id); triaged all 4 CodeQL alerts | distinguished living docs (fix) from point-in-time records (left Build-Log / ADR dates); dismissed the url-redirection alert as a verified false-positive (the allow-list makes it unreachable) rather than contort the code |
| 2026-05-30 | Harness UX series (PR #29–#32) | designed each PR + got sign-off before coding; verified backend via `import main` in CI and behaviour on the Codespace; sequenced as small PRs | found worktree subagents are read-only here (design-only, not autonomous PRs); knew the frontend PRs collide in the message template so only backend-vs-frontend splits run in parallel; deferred copy-code-block + stacked-PR verification to the Codespace (no local JS runtime) |
